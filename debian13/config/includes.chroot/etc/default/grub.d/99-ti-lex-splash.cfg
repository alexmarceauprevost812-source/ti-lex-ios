# L'amorce live reçoit « quiet splash » par lb config ; le système installé, lui,
# tient sa ligne de commande de GRUB. Sans ce fichier, l'écran de démarrage
# TI-LEX ne paraîtrait jamais après installation.
GRUB_CMDLINE_LINUX_DEFAULT="$GRUB_CMDLINE_LINUX_DEFAULT quiet splash"
